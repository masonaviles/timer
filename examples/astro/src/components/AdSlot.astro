---
// Placeholder ad slot — Phase 6 implements the pluggable strategy
// (selfPromo | network | none). Ads live in the page SHELL, never inside the timer island,
// so the tool stays reusable and ad-free by default (ADR-003).
interface Props {
  id: string;
}
const { id } = Astro.props;
---

<aside class="ad-slot" data-slot={id}>
  <!-- Phase 6: render <AdSlot strategy="selfPromo" offerings={...} /> here -->
</aside>

<style>
  .ad-slot {
    min-height: 90px; /* reserve space to avoid layout shift */
  }
</style>

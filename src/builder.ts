// Package entry "@masonaviles/cuestack/builder" — the Preact builder UI.
// Importing this pulls in Preact + the bundled CSS (sideEffects keeps it).
export { Builder, type BuilderProps, mountBuilder } from './view/index.js';

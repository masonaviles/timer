// Data layer — TimerConfig schema + version. Filled in Phase 1.
// See docs/02-ARCHITECTURE.md#core-data-model and docs/phases/phase-1-engine.md.

export const SCHEMA_VERSION = 1 as const;

// Phase 1 replaces this stub with Step / ThemeTokens / TimerConfig interfaces.
export type Placeholder = typeof SCHEMA_VERSION;

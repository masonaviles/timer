// Engine layer — the finite state machine. The heart of the app. Filled in Phase 1.
// States: IDLE -> READY -> RUNNING <-> PAUSED -> FINISHED.
// No DOM, no window, no Date.now() — time comes from the injected clock.
// See docs/02-ARCHITECTURE.md (The engine) and docs/phases/phase-1-engine.md.

export const __engineStub = true;

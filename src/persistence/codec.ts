// Persistence layer — ConfigCodec: TimerConfig <-> compressed URL param. Filled in Phase 4.
// Single tested boundary for the share-link format (LZ + URL-safe base64, size cap).

export const __codecStub = true;

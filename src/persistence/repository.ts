// Persistence layer — ConfigRepository interface + LocalStorageRepository. Filled in Phase 4.
// Repository pattern: a future ApiRepository drops in unchanged (protects "no backend now").

export const __repositoryStub = true;
